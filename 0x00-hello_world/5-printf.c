#include <stdio.h>
/**
 * main - This program prints a line using the function printf
 * Return 0 (success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}

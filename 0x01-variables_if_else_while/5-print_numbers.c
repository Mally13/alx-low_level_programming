#include <stdio.h>

/**
 * main - a program that prints all single digit numbers of base 10 starting from 0
 * return : 0(Success)
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d" , i );
	return (0);
}

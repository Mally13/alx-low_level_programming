#include <stdio.h>
/**
 * main - prints all single digit numbers starting from 0
 * return: 0 (success)
 */
int main(void)
{
	int i;

	for (i = 48; i <= 57; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
